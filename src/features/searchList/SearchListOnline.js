import { Text, View } from "react-native"

const SearchListOnline = () => {
    return (
        <View style={{ flexGrow: 1, justifyContent: 'center' }}>
            <Text style={{ color: 'rgba(0, 0, 0, 0.3)' }}>
                был(а) недавно
            </Text>
        </View>
    )
}

export default SearchListOnline
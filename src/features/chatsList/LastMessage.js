import { Text } from "react-native"

const LastMessage = () => {
    return (
        <Text style={{ color: 'rgba(0,0, 0, 0.5)', flexGrow: 1 }}>
            Привет хуй
        </Text>
    )
}

export default LastMessage
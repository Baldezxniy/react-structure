import { TouchableHighlight } from "react-native-web"
import { Ionicons } from 'react-native-vector-icons'
const SendMessageButton = ({ handleSubmit }) => {
    return (
        <></>
    )
}

export default SendMessageButton